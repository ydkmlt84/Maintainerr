This is a straight forward nginx reverse proxy setup for Maintainerr, served under a subfolder at http://localhost:8080/maintainerr
It's intended use is for testing the BASE_PATH feature locally, and as such does not persist any data nor make any attempt to be suitable for production.
